language: node_js
cache:
  yarn: true
node_js: "10"
install: yarn
script: yarn build
notifications:
  email: false
deploy:
  provider: pages
  local-dir: dist
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  keep-history: true
  on:
    branch: master
